source "https://rubygems.org"

# Matches every consuming site. github-pages pins the exact Jekyll, kramdown
# and Rouge versions GitHub Pages runs, so the preview cannot drift from
# production. jekyll-remote-theme ships inside it.
gem "github-pages"
